{% extends 'layout.html.twig' %}

{% block content %}
    <h1 class="fs-2">Hello Wilder !</h1>
    <p>Find a complete CRUD example on <a href="/items">/items</a> page.</p>

    {% for animal in animals %}

    <span>{{ animal.name }}</span>
    <span>{{ animal.characteristics }}</span>
    {% endfor %}
{% endblock %}
